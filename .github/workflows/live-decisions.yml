name: 5 - Consolidated ten-minute collector

on:
  workflow_dispatch:
  schedule:
    # Offset from the top of the hour, where GitHub documents peak scheduler load.
    - cron: "7,17,27,37,47,57 * * * *"

permissions:
  contents: write
  actions: read

concurrency:
  group: weatherman-database
  cancel-in-progress: false

jobs:
  live-decisions:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Record distinct scheduler timestamps
        run: |
          echo "WEATHERMAN_EVENT_CREATED_AT=$(gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }} --jq .created_at)" >> "$GITHUB_ENV"
          echo "WEATHERMAN_RUN_STARTED_AT=$(gh api repos/${{ github.repository }}/actions/runs/${{ github.run_id }} --jq .run_started_at)" >> "$GITHUB_ENV"
        env:
          GH_TOKEN: ${{ github.token }}
      - name: Collect every due source through the single writer
        run: >-
          bash scripts/run_database_job.sh
          "data: consolidated collector"
          python -m weatherman.cli run-collector
        env:
          METEOBLUE_API_KEY: ${{ secrets.METEOBLUE_API_KEY }}
          WEATHERMAN_FAST_DATABASE_JOB: "1"
